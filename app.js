(() => {
  "use strict";
  // See local artifacts/vex-path-planner/app.js for full source.
  // This placeholder will be replaced - open local index.html for the complete planner.
  console.error('Please use the local app.js from the project artifacts folder, or re-download after next push.');
  document.body.innerHTML = '<div style="padding:40px;font-family:sans-serif;background:#0b0f14;color:#e8eef7;min-height:100vh"><h1>Upload full app.js</h1><p>The complete planner is in your project folder: <code>artifacts/vex-path-planner/</code></p><p>Open <code>index.html</code> locally, or replace this file on GitHub with the local <code>app.js</code> (about 28 KB).</p></div>';
})();
